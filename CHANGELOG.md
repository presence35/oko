# Changelog

## [Unreleased]

- Fixed "off means off" for Sources: a disabled source no longer feeds threats/alerts or triggers the fallback — turning the primary off no longer keeps red zones/sirens alive or engages the backup, and no degraded/offline alarm fires with every source switched off / Fixed "off means off" for Sources: a disabled source no longer feeds threats/alerts or triggers the fallback — turning the primary off no longer keeps red zones/sirens alive or engages the backup, and no degraded/offline alarm fires with every source switched off

- "Show borders" now draws oblast outlines, with a nested "Regions" sub-setting that adds thinner, lighter district outlines / "Показати кордони" тепер малює контури областей, з підналаштуванням "Regions", що додає тонші контури районів
- City labels now have a "Large" chip too, so you can hide every city label (zero labels) / Підписи міст тепер мають чіп "Large", тож можна приховати всі підписи міст
- Fixed "Fill alerting regions" icon sizing — the Ukraine silhouette now fits its natural aspect ratio instead of being squished into a square / Виправлено розмір іконки "Заливка областей" — силует України тепер відповідає своєму співвідношенню сторін замість стискання в квадрат

- Fixed the merged alert feed dropping all but one alert per oblast: the registry deduped on the parent oblast name, so a whole-oblast alert and every raion inside it collapsed into a single entry and most region fills silently vanished — dedup is now on each alert's own key / Fixed the merged alert feed dropping all but one alert per oblast: the registry deduped on the parent oblast name, so a whole-oblast alert and every raion inside it collapsed into a single entry and most region fills silently vanished — dedup is now on each alert's own key
- Logs → Sources now shows the merged alert feed with each alert's key, name, oblast and wide/raion tag, so the region data is inspectable against NEPTUN's live map / Logs → Sources now shows the merged alert feed with each alert's key, name, oblast and wide/raion tag, so the region data is inspectable against NEPTUN's live map
- "Fill alerting regions" now fills a city-level alert's district too: every red city resolves to its actual raion, so a bare-city alert (no "район" in the name) shades that raion instead of leaving the red label floating on empty tiles / "Fill alerting regions" now fills a city-level alert's district too: every red city resolves to its actual raion, so a bare-city alert (no "район" in the name) shades that raion instead of leaving the red label floating on empty tiles
- Reject out-of-range threat coordinates (lat outside ±90, lon outside ±180) at parse time so a corrupted field can't place a phantom track / Reject out-of-range threat coordinates (lat outside ±90, lon outside ±180) at parse time so a corrupted field can't place a phantom track
- Ignore an absurd server-reported speed (over ~20,000 km/h) instead of trusting it, so a corrupted field can't fabricate a near-zero ETA / Ignore an absurd server-reported speed (over ~20,000 km/h) instead of trusting it, so a corrupted field can't fabricate a near-zero ETA
- A threat arriving without a timestamp no longer overwrites a properly-timestamped stored record / A threat arriving without a timestamp no longer overwrites a properly-timestamped stored record
- Connection watchdog, degraded and offline-escalation timers now run on a monotonic clock, so a wall-clock jump (auto-sync, timezone change) can't stall or trigger them / Connection watchdog, degraded and offline-escalation timers now run on a monotonic clock, so a wall-clock jump (auto-sync, timezone change) can't stall or trigger them
- A socket failure now closes the socket it failed on (not whatever is currently active), and each socket handles its disconnect exactly once / A socket failure now closes the socket it failed on (not whatever is currently active), and each socket handles its disconnect exactly once
- Test-simulator threats are now watermarked: an amber "SIMULATION" tag on the map marker, the threat card and the Sources row, so a fake threat is never mistaken for a live one / Test-simulator threats are now watermarked: an amber "SIMULATION" tag on the map marker, the threat card and the Sources row, so a fake threat is never mistaken for a live one
- Region fills now shade every area NEPTUN alerts, not just ones that happen to contain a known city — a raion/district alert fills that district even when the city list has no entry for it, matching NEPTUN's map / Region fills now shade every area NEPTUN alerts, not just ones that happen to contain a known city — a raion/district alert fills that district even when the city list has no entry for it, matching NEPTUN's map
- The "Fill alerting regions" settings icon is now much larger (56dp) / The "Fill alerting regions" settings icon is now much larger (56dp)
- Fixed raion ("district") region fills being drawn with swapped coordinates — a city-level alert now shades its raion polygon on the map instead of rendering the fill in the wrong place / Fixed raion ("district") region fills being drawn with swapped coordinates — a city-level alert now shades its raion polygon on the map instead of rendering the fill in the wrong place
- Circling threats now patrol the city they're heading to (resolved from the course text) instead of the user's zone ring — editing your yellow/red zone no longer moves or re-points an orbiting threat, and a threat that reaches the red zone parks on its actual coordinates / Circling threats now patrol the city they're heading to (resolved from the course text) instead of the user's zone ring — editing your yellow/red zone no longer moves or re-points an orbiting threat, and a threat that reaches the red zone parks on its actual coordinates
- Zoom/zone buttons have more breathing room (wider spacing and bottom padding) / Zoom/zone buttons have more breathing room (wider spacing and bottom padding)
- The camera now frames revealed threats inside the actually-visible area: the open threat card's measured height is reserved from the top, so a revealed threat never hides under it / The camera now frames revealed threats inside the actually-visible area: the open threat card's measured height is reserved from the top, so a revealed threat never hides under it
- The zone panel now fits the yellow zone into the area actually visible above the sheet (uses the sheet's real measured height instead of a fixed 40% assumption) / The zone panel now fits the yellow zone into the area actually visible above the sheet (uses the sheet's real measured height instead of a fixed 40% assumption)

- Fixed raion ("district") region fills being drawn with swapped coordinates — a city-level alert now shades its raion polygon on the map instead of rendering the fill in the wrong place / Fixed raion ("district") region fills being drawn with swapped coordinates — a city-level alert now shades its raion polygon on the map instead of rendering the fill in the wrong place
- MiG flyby no longer shows "Neutralizing threat…" in the bottom bar — the flourish footer only shows a message when a strike/death flourish is actually in flight / MiG flyby no longer shows "Neutralizing threat…" in the bottom bar — the flourish footer only shows a message when a strike/death flourish is actually in flight
- Degraded (orange) now appears the moment the NEPTUN feed stops delivering (disabled or down — silently quiet for 30s) — offline (red) only after 5 minutes with no fallback data, and the Ubilling backup starts polling on any degradation / Degraded (orange) now appears the moment the NEPTUN feed stops delivering (disabled or down — silently quiet for 30s) — offline (red) only after 5 minutes with no fallback data, and the Ubilling backup starts polling on any degradation
- Threat staleness is now source-agnostic: the map and zone alerts stay live whenever ANY source delivers fresh data, so the Test simulator (and any other source) works with NEPTUN off instead of being hidden behind NEPTUN's quiet feed / Threat staleness is now source-agnostic: the map and zone alerts stay live whenever ANY source delivers fresh data, so the Test simulator (and any other source) works with NEPTUN off instead of being hidden behind NEPTUN's quiet feed
- Fixed Test source crash ~10s after enabling: the MiG-31K event spawned a threat at an equal min/max distance, which made the random-distance picker throw and kill the app — the simulator now tolerates degenerate ranges, and a bad script event can no longer crash the app / Fixed Test source crash ~10s after enabling: the MiG-31K event spawned a threat at an equal min/max distance, which made the random-distance picker throw and kill the app — the simulator now tolerates degenerate ranges, and a bad script event can no longer crash the app
- Logs → Sources: each source now has a Test button that runs a live check and shows the result inline / Logs → Sources: each source now has a Test button that runs a live check and shows the result inline
- Test source is now a full-capability simulator: moving threats along paths, resolve/remove events, advisory, uncertainty, area-only, wave counts, raion-level alerts, aviation distance ranges — with a much richer scenario script covering all types, all tiers, the MiG flyby, locality coverage and alert clears / Test source is now a full-capability simulator: moving threats along paths, resolve/remove events, advisory, uncertainty, area-only, wave counts, raion-level alerts, aviation distance ranges — with a much richer scenario script covering all types, all tiers, the MiG flyby, locality coverage and alert clears

- Unified protection status in the header: "Reduced" chip when notifications are off, every alert channel is silenced, official alerts are off, or the critical-offline override is off — no more silently degraded monitoring / Єдиний статус захисту в шапці: чип "Reduced", коли сповіщення вимкнені, всі канали тривог вимкнено, офіційні оповіщення вимкнено або вимкнено критичний офлайн-режим — більше немає тихого зниженого захисту
- WorkManager watchdog restarts the monitoring service if the process kills it while the app is alive / Вартовий WorkManager перезапускає службу моніторингу, якщо процес було вбито системою, поки застосунок живий
- Persistent "Monitoring paused after reboot" notification when auto-restart is disabled / Постійне сповіщення "Моніторинг призупинено після перезавантаження", коли автоперезапуск вимкнено
- Amber "siren follows system volume" note in the header during a live red alert when the siren override is off / Помаранчева позначка "сирена за системною гучністю" в шапці під час активної червоної тривоги, коли примусову сирену вимкнено
- "Night zone settings active" banner in the zones sheet when night mode overrides the day toggles / Банер "Активні нічні налаштування зон" у панелі зон, коли нічний режим перевизначає денні перемикачі

- Remove Tests tab from Logs (the Test source's data is already visible on Sources) / Видалити вкладку Тести з Журналу (дані Test джерела вже видно на Sources)
- When following GPS with no fresh fix, startup now actively requests a precise fix so the location dot appears promptly on a fresh install / When following GPS with no fresh fix, startup now actively requests a precise fix so the location dot appears promptly on a fresh install
- Precise location refresh now falls back to approximate (network) fix when only coarse permission is granted / Precise location refresh now falls back to approximate (network) fix when only coarse permission is granted
- Live received-data card below each source in Sources tab — shows current threats by type and alerts by oblast, updating in real time / Картка отриманих даних під кожним джерелом на вкладці Sources — показує поточні загрози за типами та оповіщення за областями в реальному часі
- Fix Test source badge: WS → JSON / Виправити мітку Test джерела: WS → JSON
- Clean up Logs header: single row with NEPTUN icon, domain, status word and counts / Очистити заголовок Журналу: один рядок з іконкою NEPTUN, доменом, статусом та лічильниками
- Softer red on the monitor notification during a red alert — drop the red card background and 🔴 emoji, keep the red trident large icon / Пом'якшити червоний колір сповіщення моніторингу при червоній тривозі — прибрати червоний фон картки та емодзі 🔴, зберегти червоний тризуб як велику іконку
- FlourishFooter is now a fully isolated flourish-module overlay (own file, no threat-strip knowledge): the threat strip never shows flourish text anymore, a single "any flourish" boolean decides which footer renders, and flyby is treated like any other flourish / FlourishFooter тепер повністю ізольований оверлей модуля flourish (окремий файл, без знань про стрічку загроз): стрічка загроз більше ніколи не показує flourish-текст, один булевий "any flourish" визначає, який футер рендериться, а проліт обробляється як будь-який інший flourish

- Fixed "Fill alerting regions" not drawing oblast fill polygons — the map now correctly shades alerting oblasts when the toggle is on / Виправлено відсутність заливки областей при увімкненій опції "Заповнювати регіони з тривогою" — карта тепер коректно підсвічує області з тривогою
- Icon-pack rows now scroll all the way to the 7th icon (RECON), which was previously unreachable past the 6th; the "Fill alerting regions" toggle now shows a red Ukraine silhouette / Icon-pack rows now scroll all the way to the 7th icon (RECON), which was previously unreachable past the 6th; the "Fill alerting regions" toggle now shows a red Ukraine silhouette
- "Fill alerting regions" now shades alerting districts too, not just whole oblasts — a Dnipro raion alert fills the raion on the map instead of only reddening the city name, and the toggle shows a red Ukraine silhouette / "Fill alerting regions" now shades alerting districts too, not just whole oblasts — a Dnipro raion alert fills the raion on the map instead of only reddening the city name, and the toggle shows a red Ukraine silhouette
- Every red city now sits on a filled region: region/city alerts resolve to the city's actual district polygon (bare-city alerts included), so a city colored red is always backed by a map fill instead of floating on empty tiles / Every red city now sits on a filled region: region/city alerts resolve to the city's actual district polygon (bare-city alerts included), so a city colored red is always backed by a map fill instead of floating on empty tiles
- The map's country outline now follows only the land border — it hugs the coast and river borders and no longer draws a white line across the sea / The map's country outline now follows only the land border — it hugs the coast and river borders and no longer draws a white line across the sea

- One emergency-eject stop covers every playful animation: it's available from the auto-strike 3-2-1 countdown, during the shoot-down (auto or long-press), during the tally replay, and during the MiG flyby (which now cancels without opening the card) — tap the whole strip / One emergency-eject stop covers every playful animation: it's available from the auto-strike 3-2-1 countdown, during the shoot-down (auto or long-press), during the tally replay, and during the MiG flyby (which now cancels without opening the card) — tap the whole strip
- Circling threats now point along their patrol ring — fast types circle the red ring, slow ones the yellow — and touching the map no longer stalls moving markers / Circling threats now point along their patrol ring — fast types circle the red ring, slow ones the yellow — and touching the map no longer stalls moving markers
- Just Fun master switch now also resets calm messages to the default text / Just Fun master switch now also resets calm messages to the default text
- Icon zoom moved from Just Fun to System & Display / Icon zoom moved from Just Fun to System & Display
- Flyby sub-toggle now actually works / Flyby sub-toggle now actually works
- City labels reveal progressively: the fully zoomed-out country view shows only the 5 largest cities (Kyiv, Kharkiv, Odesa, Dnipro, Lviv), 8 more majors appear as you zoom in, and the rest up close — no more cluttered overview / Підписи міст з'являються поступово: на загальнонаціональному вигляді лише 5 найбільших міст (Київ, Харків, Одеса, Дніпро, Львів), ще 8 обласних центрів — при наближенні, решта — зблизька; огляд карти більше не перевантажений
- Official-alert text now transliterates on the English path — the notification body reads "Luhanska oblast" instead of raw Cyrillic / Official-alert text now transliterates on the English path — the notification body reads "Luhanska oblast" instead of raw Cyrillic
- Map and notifications now share the same official-alert scope gate: an oblast-wide alert lights every city in the region and rings anywhere in it (Crimea matches by word too) / Map and notifications now share the same official-alert scope gate: an oblast-wide alert lights every city in the region and rings anywhere in it (Crimea matches by word too)
- Fixed "Fill alerting regions" — oblast fills were drawn with swapped coordinates, so the toggle had no visible effect; now alerting oblasts shade correctly / Fixed "Fill alerting regions" — oblast fills were drawn with swapped coordinates, so the toggle had no visible effect; now alerting oblasts shade correctly
- New Test source (Logs → Sources): a server-driven simulator (testplugin.json on the update server) that plays timed threat/alert scenarios while enabled — random threat bursts, a MiG-31K takeoff that triggers the flyby, fake oblast alerts, and clear-by-disable. Real sources keep running alongside unless you switch them off / New Test source (Logs → Sources): a server-driven simulator (testplugin.json on the update server) that plays timed threat/alert scenarios while enabled — random threat bursts, a MiG-31K takeoff that triggers the flyby, fake oblast alerts, and clear-by-disable. Real sources keep running alongside unless you switch them off
- Decluttered city labels at mid-zoom: auto-promoted 50k-population cities no longer crowd the country view — MEDIUM is the curated set only / Decluttered city labels at mid-zoom: auto-promoted 50k-population cities no longer crowd the country view — MEDIUM is the curated set only
- Settings System subtitle now reads "icons: <name>" so an icon-pack name can't be mistaken for the UI language; the Alerts subtitle shows its ON/OFF state again / Settings System subtitle now reads "icons: <name>" so an icon-pack name can't be mistaken for the UI language; the Alerts subtitle shows its ON/OFF state again
- Icon-pack picker redesigned: enlarged icons, swipeable rows, a half-peek on the right-most icon, and a subtle pack-name badge in the row corner / Icon-pack picker redesigned: enlarged icons, swipeable rows, a half-peek on the right-most icon, and a subtle pack-name badge in the row corner
- Logs: Decisions default to timeline + shown-only; official-alert rows show the region (city/oblast); the top-right X is gone (swipe/back closes); the header shows a live health word and threat/alert counts, with the NEPTUN mark tinted by the real connection state / Logs: Decisions default to timeline + shown-only; official-alert rows show the region (city/oblast); the top-right X is gone (swipe/back closes); the header shows a live health word and threat/alert counts, with the NEPTUN mark tinted by the real connection state

- Threat icons no longer slide along with your finger while panning the map — they stay locked to the ground until you lift your finger / Threat icons no longer slide along with your finger while panning the map — they stay locked to the ground until you lift your finger
- Threat markers now point along their true course and glide smoothly between server fixes instead of snapping / Threat markers now point along their true course and glide smoothly between server fixes instead of snapping
- Removed the always-on 1-second UI clock — the GPS fix-age label now updates only while the shelter screen is open / Removed the always-on 1-second UI clock — the GPS fix-age label now updates only while the shelter screen is open
- Auto-resolution countdown: server-driven shoot-downs show a 3-second "3 2 1" targeting lock (with the target type + remaining threat count) in a footer strip before firing, and a "tap to cancel" hint stays up for the whole animation — tap anywhere on the strip to cancel the countdown or eject the animation early and keep the map where it is. The floating zone/shelter buttons hide during the moment / Зворотний відлік автоматичного знищення: серверні збиття показують 3-секундне захоплення цілі «3 2 1» (з типом цілі та кількістю загроз) у смужці перед пострілом, а підказка «торкніться, щоб скасувати» лишається на весь час анімації — торкніться будь-де смужки, щоб скасувати відлік або перервати анімацію. Кнопки зон/сховищ ховаються на цей час
- Scale bar on the map now uses the same muted color as the CARTO attribution / Шкала масштабу на карті тепер використовує той самий приглушений колір, що й атрибуція CARTO
- Shoot-down animation zooms out a bit more for projectile context; replay flourish groups are larger / Анімація збиття трохи більше віддаляє карту для контексту траєкторії; групи повторного шоу більші
- New Settings toggle "Fill alerting regions" — when on, oblasts with active air-raid alerts are shaded with a subtle red polygon fill instead of tinting city labels red; toggle is in the System section below the City labels row / Новий перемикач «Заливка областей» — коли увімкнено, області з активними тривогами заливаються напівпрозорим червоним полігоном замість підсвітки червоним підписів міст; перемикач у системному розділі під рядком «Міста на карті»
- Monitoring is always-on: "Stop Monitoring & Exit" is now session-only (a cold start re-arms the service), and a new Settings toggle "Restart monitoring after reboot" (with a security warning) controls boot auto-start. If the monitor dies while the app is open, the whole header becomes a red "SERVICE OFFLINE — tap to reactivate" banner instead of silently losing alerts while the map keeps working.
- Ubilling structural schema fingerprint: every REST poll hashes the JSON key shape and compares it to the last-known fingerprint; a shift (API renames/removes/adds fields) is logged in System and pushed to Telegram — same pattern as the NEPTUN SDK manifest check / Структурний відбиток схеми Ubilling: кожне REST-опитування хешує форму JSON-ключів і порівнює з останнім відомим відбитком; зміна (API перейменовує/видаляє/додає поля) фіксується в системному лозі та надсилається в Telegram — той самий патерн, що й перевірка маніфесту NEPTUN SDK
- New Ubilling REST source: when the primary WS source is down past a 5-min grace, Ubilling takes over official-oblast alerts with battery-aware adaptive polling (15s foreground / 30s background), auto-clearing on recovery — no false all-clear, no double-notify / Нове REST-джерело Ubilling: коли основне WS-джерело недоступне понад 5 хв, Ubilling бере на себе офіційні тривоги областей з енергоощадним адаптивним опитуванням (15с у фоні активного / 30с у фоні), автоматично очищаючись при відновленні — без фальшивого «відбій», без подвійних сповіщень
- Logs Sources tab: per-source toggle actually sticks (bound to real enabled state, not derived from mode); fallback takeovers logged in the connection log / Вкладка «Джерела» в журналі: перемикач джерела тепер працює (прив'язаний до реального стану), перехід на резервне джерело фіксується у журналі з'єднань
- Logs Sources tab: Test button per source runs a live check (REST fetch / WS state) and shows the result inline; a live activity feed lists source toggles and alert-owner handovers / Вкладка «Джерела»: кнопка «Тест» для кожного джерела виконує живу перевірку (REST-запит / стан WS) і показує результат одразу; жива стрічка активності відображає перемикання джерел та переходи власника тривог
- Removed 20s all-clear grace period — alerts clear instantly / Видалено 20с затримку попередження — сповіщення зникають миттєво
- Explosion now scales with map zoom level / Вибух тепер масштабується з рівнем зуму карти
- Shot-down drones hide during death animation then scale back in after 2.1s / Збиті дрони ховаються під час анімації знищення і повертаються за 2.1с
- Fixed threat icon flipping direction when shot / Виправлено перевертання іконки загрози при пострілі
- Alerts never fall back to a pinned city while following GPS — the last known GPS fix is used even when stale; no fix at all shows a persistent "No GPS fix" warning / Alerts never fall back to a pinned city while following GPS — the last known GPS fix is used even when stale; no fix at all shows a persistent "No GPS fix" warning
- Map panning now extends past Ukraine's tight bounds so the viewport can shift content out from under the threat card when zoomed at a country edge; added a subtle outline of Ukraine's perimeter / Map panning now extends past Ukraine's tight bounds so the viewport can shift content out from under the threat card when zoomed at a country edge; added a subtle outline of Ukraine's perimeter
- Fixed several Settings toggles reading each other's state after "Fill alerting regions" was inserted into the wrong spot of the prefs snapshot — "Just Fun", "Critical offline alert", the Fast/Slow collapse carets and "Fill alerting regions" now toggle their own stored values again / Fixed several Settings toggles reading each other's state after "Fill alerting regions" was inserted into the wrong spot of the prefs snapshot — "Just Fun", "Critical offline alert", the Fast/Slow collapse carets and "Fill alerting regions" now toggle their own stored values again
- Pinning "Миколаїв" now lands on the Mykolaiv oblast capital instead of the small town near Lviv — same-named cities resolve to the major city first, then by population / Pinning "Миколаїв" now lands on the Mykolaiv oblast capital instead of the small town near Lviv — same-named cities resolve to the major city first, then by population
- Logs Sources tab no longer shows a bogus "No sources plugged in" empty-state under the real source cards; the Tests tab now lists live per-source test results (with timestamps) instead of a misplaced battery simulator and the Connections empty message; the OEM battery simulator moved to the System tab / Logs Sources tab no longer shows a bogus "No sources plugged in" empty-state under the real source cards; the Tests tab now lists live per-source test results (with timestamps) instead of a misplaced battery simulator and the Connections empty message; the OEM battery simulator moved to the System tab
- Threat markers now hold their last reported position once a track goes stale or the source reports no course — dead-reckoning only glides fresh threats the server itself is moving
- Official-alert clears are debounced (30s): a momentary feed gap can't flip an active alert off and back on, and a quiet socket no longer owns the alert feed — no more random on/off
- The auto-shootdown no longer wipes the red alert fill, and its countdown shows the number of strikes being fired instead of a leftover 0
- Cancelling or ejecting an auto shoot-down returns the camera to where you were immediately, then cleans up behind the scenes
- Tapping "threats resolved" now replays promptly even when the app was already on screen; re-sent resolutions no longer re-strike or re-count ~1 minute later
- Map opens at the country view and zooms into your zone on the first fix — the jarring startup "fully in, then fully out" dance is gone
- Ukraine's outline now matches the outer edge of the oblast fills (same source, same simplification), so the red alert fill no longer shows a coastline gap at the sea
- Red coloring is now region-precise: NEPTUN's whole-oblast alerts shade/fill the entire region, but a raion/city alert only marks the cities it actually covers — no more entire oblast lit for one district. The "Fill alerting regions" fill follows the same rule (whole-oblast alerts only)
- The alert feed now keeps NEPTUN's raions/oblasts split (whole-oblast vs region) instead of guessing from the region name — so e.g. a Crimea-wide alert and a single raion are treated exactly as NEPTUN reports them
- The tally-tap replay shows a Stop button (tapping it ejects the show and returns the camera home); the auto-shootdown strip is display-only again — you can just pan away from an auto-strike
- Fixed a crash when toggling the Test source on: a MiG-31K flyby racing the live feed no longer NPEs — the flyby reads the snapshot it was handed instead of the live mutable map
- Threat icons reach their full size only in the final ~3 zoom levels — no more comically large markers at mid-zoom; at normal view zooms they stay small
- Dead-reckoning is capped by distance (~5km from the last confirmed fix), not time — a marker never appears to cross the country while the source sits quiet
- Raion-level alerts now light every city in the raion when "Fill alerting regions" is on, and every city in the oblast when it's off — matching NEPTUN's region coverage (Dnipro shows its whole district, not just the city)
- English region names translate "район" to "district" instead of transliterating to "raion"
- The tally replay's Stop control is now a full-width bar, easy to see and tap
- Icon packs moved from Just Fun into the System section (always available, not gated by the Just Fun switch); the tiles are bigger so the row scrolls meaningfully; "Choose your favourite threat icons" dropped its "!"
- The ongoing monitor notification now shows a 🔴 dot (and a red-tinted card on devices that support colorized notifications) while an official alert or an INNER-zone threat is active
- New "Overlapping threats" display setting (System): Default / Grid / Spread / Count — same-coordinate markers either stay stacked, spread side-by-side on a grid, fan out half-overlapping in a row, or collapse into a counted representative (mixed threat types in one spot auto-grid so each type stays visible)
- The tally replay's Stop control is a compact button on the left; the whole footer strip is tappable to stop the show / Logs Sources tab no longer shows a bogus "No sources plugged in" empty-state under the real source cards; the Tests tab now lists live per-source test results (with timestamps) instead of a misplaced battery simulator and the Connections empty message; the OEM battery simulator moved to the System tab

